# Example 03: Container Task

Demonstrates a task defined for container execution.

Note: The reference implementation v1.0.0 does not include full container
support. Container tasks fall back to native execution with a warning.

For full container support, use a container-capable executor extension.
